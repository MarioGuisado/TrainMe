
export enum Rendimiento{
    REGIONAL,
    NACIONAL,
    INTERNACIONAL, 
};

export enum Compromiso{
    BAJO,
    MEDIO,
    ALTO, 
};

export enum ModalidadEntreno{
    PRESENCIAL,
    SEMIPRESENCIAL,
    ONLINE, 
};

export enum TipoContacto{
    WHATSAPP,
    TELEGRAM,
    EMAIL, 
};

export enum TipoMatriz{
    ENTRENADORES,
    ATLETAS,
}

export enum OpcionFiltro{
    Rendimiento = 1,
    Compromiso = 2,
    Modalidad = 3,
    Contacto = 4,
}

